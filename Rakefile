require 'rspec/core/rake_task'
task :default => [:spec]


RSpec::Core::RakeTask.new(:spec)


#task :test do
#  ret = true
#  Dir["test/**/*.rb"].each do |f|
#    ret = ret && ruby(f, '')
#  end
#  exit(ret)
#   end
